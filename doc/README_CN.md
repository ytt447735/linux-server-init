# 🚀 Linux Server Init

> 一个用于快速初始化和管理 Linux 服务器的 **CLI 工具**。安装后可在任意位置通过 `server-init` 命令调用，支持在线更新。

[English](README_EN.md) | **中文**

---

## ✨ 功能特性

- **🐧 多系统支持**：CentOS 7/8/Stream 9, Ubuntu 20.04+, Debian 10+
- **🖥️ 系统信息总览**：主机名、OS、CPU、内存、磁盘、IP、负载
- **🔄 系统换源**：一键替换阿里云镜像源（自动备份）
- **📦 系统更新**：全量更新 + 重启提示
- **🧰 常用工具安装**：基础包 + 增强运维包（vim, htop, tmux, jq 等）
- **🐳 Docker 安装**：Docker CE + 国内镜像加速
- **📦 常用服务安装**：Nginx / Node.js / Python / Go / GCC / Docker Compose（原生 + Docker 双模式）
- **⏰ NTP 校时**：7 家国内 NTP 源，支持 chrony / ntpdate
- **🛡️ 防火墙管理**：firewalld / ufw 端口管理
- **🔐 SSH 安全加固**：密钥登录、改端口、禁止密码/Root 登录
- **👤 用户账号管理**：增删用户、密码、sudo 权限
- **⚙️ 服务管理**：systemd 启停重启、自启、日志、端口查看
- **⏰ 定时任务管理**：Crontab 增删改查 + 8 种任务模板
- **🧹 系统清理**：缓存/日志/内核/依赖 + Docker 8 种清理模式
- **🌐 中英文切换**：主菜单支持中/英文实时切换
- **🔧 自管理**：菜单内更新 / 修复 / 卸载 / 查看版本

---

## 🖥️ 支持的操作系统

| 系统   | 版本                   | 状态 |
| :----- | :--------------------- | :--- |
| CentOS | 7.x / 8.x / Stream 8/9 | ✅    |
| Ubuntu | 20.04 / 22.04 / 24.04  | ✅    |
| Debian | 10 / 11                | ✅    |

---

## 📺 界面预览

```
  ┌──────────────────────────────────────────────┐
  │        🚀 Linux Server Init  v1.0.0          │
  │        CentOS/RHEL 服务器初始化工具          │
  └──────────────────────────────────────────────┘
  CentOS Stream 9  ·  my-server

  ▸ 信息
     1. 系统信息总览

  ▸ 系统配置
     2. 更换阿里源       4. NTP 校时
     3. 系统更新         5. 主机名 / 时区设置

  ▸ 软件安装
     6. 常用工具安装     8. 常用服务安装
     7. 安装 Docker

  ▸ 安全管理
     9. 防火墙设置      11. 用户账号管理
    10. SSH 安全加固

  ▸ 运维管理
    12. 服务管理        14. 系统清理
    13. 定时任务管理

  ──────────────────────────────────────────────
    u) 更新   v) 版本   r) 修复   x) 卸载   l) EN
  ──────────────────────────────────────────────

     0. 退出

  请选择 [0-14/u/v/r/x/l]:
```

---

## 🚀 安装

以 **root** 用户执行：

### 国内服务器（推荐）

```bash
curl -fsSL https://gh-proxy.org/https://github.com/ytt447735/linux-server-init/raw/refs/heads/main/install.sh -o /tmp/install.sh && bash /tmp/install.sh
```

### 海外服务器

```bash
curl -fsSL https://raw.githubusercontent.com/ytt447735/linux-server-init/main/install.sh -o /tmp/install.sh && bash /tmp/install.sh
```

### git clone

```bash
git clone https://github.com/ytt447735/linux-server-init.git && cd linux-server-init && bash install.sh
```

---

## 📖 使用方法

安装后可在任意位置使用 `server-init` 命令：

```bash
server-init              # 启动主菜单
server-init update       # 在线更新到最新版本
server-init uninstall    # 卸载工具
server-init version      # 查看版本信息
server-init help         # 查看帮助
```

| 路径                          | 说明         |
| ----------------------------- | ------------ |
| `/usr/local/bin/server-init`  | CLI 命令入口 |
| `/usr/local/lib/server-init/` | 脚本安装目录 |
